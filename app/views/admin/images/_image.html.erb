<li>
  <%= content_tag :div, id: "image-#{image.id}" do -%>
    <b><%= link_to image.title, admin_image_path(image) %>:</b>
    <%= simple_form_for image, url: admin_image_url(image), method: :patch do |f| %>
      <%= f.input :title %>
      <%= f.submit %>
    <% end %>
    <%= link_to 'Smazat obrázek', admin_image_path(image), method: :delete %>
    <br />
    ====================
  <% end -%>
</li>
