# require 'spec_helper'
#
# describe TextPost do
#  it { expect(subject).to have_many(:image) }
#  it { expect(subject).to have_many(:video) }
#  it { expect(subject).to validate_presence_of(:text) }
#  it { expect(subject).to ensure_length_of(:text).is_at_least(300) }
# end
